// Prisma schema for Agenticket.
// Models will be added in Phase 2 (ticket management) onwards.
// See /implementation-plan.md for the model rollout plan.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
